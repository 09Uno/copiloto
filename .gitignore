__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Credenciais. `.env*` (e não só `.env`) porque a extensão Local History do VS Code salva
# cópias como `.env_20260714133052` — e elas JÁ ENTRARAM num commit uma vez, com a senha do
# Supabase e a do FinControl dentro.
.env
.env.*
!.env.example
.history/

# Dado bruto ingerido: reprodutível via `dands backfill`, não versionar
data/

node_modules/
dist/
.DS_Store
